<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">Silex skeleton</a>
    <ul class="navbar-nav mr-auto">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <li class="nav-item">
                <span class="navbar-text">{{ app.user.username }}</span>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('logout') }}">Logout</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a href="{{ path('silex_user.login') }}" class="nav-link">Login</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('silex_user.register') }}" class="nav-link">Register</a>
            </li>
        {% endif %}
    </ul>
</nav>
